{
  "name": "Ciao Prolog dev container",

  "build": {
    "dockerfile": "Dockerfile"
  },

  "postCreateCommand": "/workspaces/ciao-boot.sh boot-build && /workspaces/ciao-boot.sh fetch devenv && /workspaces/ciao-boot.sh configure --ciaopp:lite=yes && /workspaces/ciao-boot.sh build && /workspaces/ciao-boot.sh install",

  "customizations": {
    "vscode" : {
      "extensions" : [ "ciao-lang.ciao-prolog-vsc" ]
    }
  }
}